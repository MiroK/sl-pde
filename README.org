** Sparse learning PDEs (with FEniCS/Basilisk generated data)

*** Structure
    - `/src/c` is the C code related to Basilisk data generation
    - `/src/python` is the python code for data generation in FEniCS and sparse learning

